" Gerenciador de plugin
execute pathogen#infect()

" Esquema de cores
colorscheme tokyo-metro

" Como a tab
set tabstop=2

" Quantos espaços a tab ocupa
set shiftwidth=2

" configuração que identifica o tipo de arquivo aplica plugin e identação
filetype plugin indent on

" colore de acordo com a sintaxe
syntax on

" permitir que o backspace apague coisas
" set backspace=2

 " mostra o número da linha
set number

" a  contagem da linha é relativa à linha atual
set relativenumber

" destaca palavras encontradas durante a busca
set incsearch

" destaca os resultados encontrados durante uma busca
set hlsearch
